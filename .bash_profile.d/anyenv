# anyenv
export PATH
export PATH="$HOME/.anyenv/bin:$PATH"
eval "$(anyenv init -)"
#export ANYENV_ROOT=$HOME/.anyenv
#eval "$(PATH="$ANYENV_ROOT/libexec:$PATH" $ANYENV_ROOT/libexec/anyenv-init - --no-rehash)"
